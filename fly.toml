app = "vscu-evopay" 
primary_region = "fra" 
 
[[services]] 
  protocol = "tcp" 
  internal_port = 8090 
  processes = ["app"] 
 
  [[services.ports]] 
    port = 443 
    handlers = ["tls", "http"] 
 
  [[services.ports]] 
    port = 80 
    handlers = ["http"] 
